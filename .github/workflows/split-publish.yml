---

# Splits plugin/bridge subtrees into their own dedicated repositories
# whenever release-please tags a new component release in the monorepo.
#
# Each job:
#   - watches for tags matching its component prefix (e.g. "repeat-0.1.0",
#     produced by release-please thanks to per-package include-component-in-tag);
#   - extracts the bare semver portion of the tag (e.g. "0.1.0");
#   - uses symplify/monorepo-split-github-action to push the subdirectory
#     into the target repo with that bare tag, so Packagist sees a normal
#     0.1.0 release.
#
# Tag format follows the project convention `include-v-in-tag: false`, so
# tags carry the bare version without a `v` prefix on both ends (monorepo
# and split repos).
#
# References:
#   - https://github.com/symplify/monorepo-split-github-action
#   - https://github.com/googleapis/release-please/blob/main/docs/customizing.md#subpackages
#
# Required secrets:
#   - RELEASE_TOKEN: a GitHub token (PAT or fine-grained) with `contents: write`
#     access to every target repo listed below. Same secret is reused for release-please.

on:  # yamllint disable-line rule:truthy
  push:
    tags:
      - 'repeat-[0-9]*'
      - 'convention-[0-9]*'
      - 'lifecycle-[0-9]*'
      - 'retry-[0-9]*'
      - 'filter-[0-9]*'
      - 'test-[0-9]*'
      - 'assert-[0-9]*'
      - 'data-[0-9]*'
      - 'inline-[0-9]*'
      - 'bench-[0-9]*'
      - 'codecov-[0-9]*'
      - 'output-teamcity-[0-9]*'

name: 📦 Split publish

jobs:
  split-repeat:
    name: ↪ plugin/repeat → php-testo/repeat
    if: startsWith(github.ref, 'refs/tags/repeat-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#repeat-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        # The tag is created on a major-version branch (1.x, 2.x, …).
        # Push the subtree into the same branch name on the target repo so
        # major series stay isolated end-to-end.
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/repeat
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/repeat
          repository_organization: php-testo
          repository_name: repeat
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-convention:
    name: ↪ plugin/convention → php-testo/convention
    if: startsWith(github.ref, 'refs/tags/convention-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#convention-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/convention
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/convention
          repository_organization: php-testo
          repository_name: convention
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-lifecycle:
    name: ↪ plugin/lifecycle → php-testo/lifecycle
    if: startsWith(github.ref, 'refs/tags/lifecycle-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#lifecycle-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/lifecycle
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/lifecycle
          repository_organization: php-testo
          repository_name: lifecycle
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-retry:
    name: ↪ plugin/retry → php-testo/retry
    if: startsWith(github.ref, 'refs/tags/retry-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#retry-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/retry
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/retry
          repository_organization: php-testo
          repository_name: retry
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-filter:
    name: ↪ plugin/filter → php-testo/filter
    if: startsWith(github.ref, 'refs/tags/filter-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#filter-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/filter
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/filter
          repository_organization: php-testo
          repository_name: filter
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-test:
    name: ↪ plugin/test → php-testo/test
    if: startsWith(github.ref, 'refs/tags/test-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#test-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/test
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/test
          repository_organization: php-testo
          repository_name: test
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-assert:
    name: ↪ plugin/assert → php-testo/assert
    if: startsWith(github.ref, 'refs/tags/assert-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#assert-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/assert
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/assert
          repository_organization: php-testo
          repository_name: assert
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-data:
    name: ↪ plugin/data → php-testo/data
    if: startsWith(github.ref, 'refs/tags/data-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#data-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/data
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/data
          repository_organization: php-testo
          repository_name: data
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-inline:
    name: ↪ plugin/inline → php-testo/inline
    if: startsWith(github.ref, 'refs/tags/inline-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#inline-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/inline
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/inline
          repository_organization: php-testo
          repository_name: inline
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-bench:
    name: ↪ plugin/bench → php-testo/bench
    if: startsWith(github.ref, 'refs/tags/bench-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#bench-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/bench
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/bench
          repository_organization: php-testo
          repository_name: bench
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-codecov:
    name: ↪ plugin/codecov → php-testo/codecov
    if: startsWith(github.ref, 'refs/tags/codecov-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#codecov-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/codecov
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/codecov
          repository_organization: php-testo
          repository_name: codecov
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  split-output-teamcity:
    name: ↪ plugin/output-teamcity → php-testo/output-teamcity
    if: startsWith(github.ref, 'refs/tags/output-teamcity-')
    runs-on: ubuntu-latest
    steps:
      - name: ⤵ Checkout monorepo (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏷 Extract bare version from component tag
        id: tag
        run: echo "version=${GITHUB_REF_NAME#output-teamcity-}" >> "$GITHUB_OUTPUT"

      - name: 🌿 Resolve source branch for the tag
        id: branch
        run: |
          branch=$(git branch --remotes --contains "${GITHUB_REF_NAME}" \
                    | sed 's|origin/||' \
                    | grep -v 'HEAD' \
                    | head -n 1 \
                    | tr -d ' ')
          echo "name=${branch}" >> "$GITHUB_OUTPUT"

      - name: 🚀 Push subtree to php-testo/output-teamcity
        uses: symplify/monorepo-split-github-action@v2.3.0
        with:
          tag: ${{ steps.tag.outputs.version }}
          package_directory: plugin/output-teamcity
          repository_organization: php-testo
          repository_name: output-teamcity
          branch: ${{ steps.branch.outputs.name }}
          user_name: testo-bot
          user_email: bot@testo.dev
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

...
